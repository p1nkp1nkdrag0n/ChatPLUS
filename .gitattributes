# Keep synthetic evaluation inputs stable across Windows and Unix checkouts.
apps/server/src/test-fixtures/companion-continuity/** -text

# The Android wrapper must remain executable with Unix line endings.
apps/android/gradlew text eol=lf
