# 角色问答创建实施与验收

实施日期：2026-09-11。

## 已实现的流程

Welcome 从服务端读取已发布的用户角色，决定显示「描述你梦中的他/她」或「继续聊天」。未完成的问答和未确认小传可恢复；服务器读取失败提供重试，离线暂停不按空库处理。导入后发布的角色也启用聊天。最后一个角色归档后恢复创建邀请。

服务器启动不再播种系统示例，旧 SEED_DEMO 配置也不启用播种。公开的示例创建接口返回 410。033 迁移只归档来源或示例注册记录明确识别的角色，保留角色和会话数据，不按角色姓名识别。

`/create` 使用独立书桌场景，按固定顺序呈现十二题。性别、姓名、文字年龄、世界、身份和性格必答；其余可略过。主问题完成后至多生成两个可选追问。浏览器存储保留原文、题号、追问缓存、请求标识和服务器草稿版本。修改答案会使对应旧追问失效。

编译首次建立草稿，后续修改原位更新同一未发布草稿。服务端恢复明确的作者设定，保存版本化访谈来源。预览由可追溯事实组成，小传本身不会作为新增事实写回角色。性别和文字年龄兼容旧角色，并进入编辑器和聊天上下文。

`/characters/:id/preview` 可刷新恢复。发布成功后才更新当前角色并进入聊天。冲突重新读取当前版本，必须确认的模型设定逐项处理。详细编辑或审核已经改变了访谈生成版本时，继续提供详细编辑入口，避免重新编译旧访谈覆盖这些修改。

## 视觉验收记录

按相邻的问答和小传参考图在 1672×941 尺寸逐张比较，并检查 390×844 窄屏。整体视觉匹配自评 9/10；下述差异保留为明确记录，不视为逐像素复刻。

| 项目       | 检查结果                                                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 场景比例   | 保留左窗山湖、墙面光斑、木桌、右侧雏菊；原构图的大区域位置一致。                                                                           |
| 纸张与道具 | 四份独立 PNG 图层；纸张、墨水和羽毛使用真实透明度，无黑底、棋盘底或明显光晕。纸叠边缘和羽毛细节来自独立重绘，与概念图有细微区别。          |
| 文字与按钮 | 深绿色文字、原应用字体和圆角按钮。问题、回答和小传均为 DOM；实际输入字形沿用应用字体，与概念图中绘制的手写答案不同。                       |
| 羽毛笔动作 | 问题逐字显现，笔尖跟随文字边界；换题 880ms 蘸墨与换纸，加最多约680ms书写。上一问立即显示，点击问题或纸面可完成显现。减少动画模式立即切换。 |
| 桌面阅读   | 小传姓名和主要操作固定，正文在纸面内部滚动。修改选择器和审核内容自动滚动到可见位置。更多设定可独立滚动。                                   |
| 手机阅读   | 调整纸面比例和顶部留白，长小传自然纵向阅读；没有横向溢出。                                                                                 |

浏览器证据存于本地 `C:/Users/34080/.codex/qa/character-interview-server/`：`age-desktop.png`、`preview-desktop.png`、`age-mobile.png`、`preview-mobile.png`、`preview-mobile-full.png` 和 `visual-evidence.json`。这些是独立 fixture 数据库的实际页面截图，未在用户数据库中创建测试角色。四页截图运行的 console warning/error 和 pageerror 均为 0。

## 验证

- 全仓 `pnpm typecheck` 通过。
- 全仓 `pnpm lint` 通过。
- Web 生产构建通过；Vite 仍报告主包超过 500KB 的体积提示。
- 十二题与追问、恢复、草稿版本、发布、欢迎页、旧聊天流程：1440×900 与 1920×1080 两组共 48 个浏览器用例通过；最新生命周期调整后，新增问答套件 6/6 再次通过。
- 390×844 手机、长文本、中文 IME、减少动态效果包含在上述浏览器用例内。
- 浏览器草稿恢复单元测试 20 项通过；Welcome 和会话选择前端测试 24 项通过。
- 服务端及共享数据层完成访谈生命周期、作者来源、默认示例退休、旧数据及聊天提示等聚焦回归；访谈生命周期 8 项及新 contracts 2 项均通过。

模型相关自动化使用 fixture provider 验证，不代表对外部付费模型返回内容的人工质量评估。
