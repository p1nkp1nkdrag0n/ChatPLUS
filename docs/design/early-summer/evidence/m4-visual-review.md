# M4 日常界面视觉复核

2026-09-08。基准为 `../references/primary-user-reference.png` 下半部，以及
`../IMPLEMENTATION.md` 的日常界面范围。已用 `view_image` 在同一次复核中读取主参考图、最终桌面聊天、手机聊天和桌面信件截图。

## 实际环境与证据

日常数据通过仓库 `tests/e2e/global-setup.ts` 的隔离 fixture 初始化：临时数据库、FakeClock、fixture 模型，无真实用户数据。CUA/Edge 用于真实页面和操作检查；现有 Playwright 流程用于保存可复查的截图文件，Chromium，DPR 1。

| 证据 | 画幅 | 状态 |
| --- | --- | --- |
| `m4-final-chat-desktop-1440.png` | 1440 × 1000 | 真实 fixture 会话，状态栏收起 |
| `m4-final-chat-mobile-390.png` | 390 × 844 | 真实 fixture 会话，五项底部导航 |
| `m4-final-letter-desktop-1440.png` | 1440 × 1000 | 真实用户方向草稿信件 |

截图中的对话回复来自确定性 fixture，不代表真实模型的对话质量。当前实例地址为 `http://127.0.0.1:43179`，仅用于本次本地验收。

## 对照记录

| 维度 | 主图依据 | 实现复核与处理 |
| --- | --- | --- |
| 色温 | 暖白应用纸面与浅灰绿导航 | canvas `#f3f1e9`，surface `#fcfaf5`，navigation `#edeee5`；接入 512px 轻纸纹，避免焦黄旧纸 |
| 按钮 | 深绿主操作、圆形发送 | 主色 `#345846`，发送按钮 42px；手机邮箱笔图标的 padding 挤压问题已修复 |
| 对话 | 浅绿用户气泡、白色回复、舒展中文 | 正文 15px，桌面 1.95 / 手机 1.85 行高；保留真实消息与时间，未添加图片上传或头像 |
| 纸页 | 衬线标题、开放行距、轻页边阴影 | 棉纸使用真实 paper.webp 替换原 CSS 条纹；正文行高 2.1，邮戳改为灰绿 ChatPLUS |
| 信息密度 | 以对话和信件正文为阅读核心 | 聊天默认收起状态栏，可明确展开；移除无行为上下文按钮与标题旁级别标签 |
| 导航 | 克制线性图标与低对比选中区域 | 延续现有业务路径，增对话入口；修复首次深链接挂载时错过 active-character 事件造成漏导航 |
| 响应式 | 轻量手机阅读界面 | 390×844 聊天和信件、360px 邮箱、390×500 短屏均实际检查；短屏输入框 bottom 427，导航 top 438 |
| 字符与功能边界 | 计划排除头像、图片消息与额外英文标语 | 仅使用实际业务内容与明确合成 fixture；不复制参考图中的未批准附加功能 |

日常界面继续使用现有业务壳、正文来源与阅读状态。主图中的多联系人头像列未作为新功能引入。视觉范围是纸面、配色、排版与反馈，而非重新实现用户数据库或角色管理流程。

## 操作与代码验证

- CUA 验证聊天状态栏展开/收起、信件清晰阅读切换和邮箱导航；有效页面控制台 error/warn 为空。
- 360px 邮箱无横向溢出，笔图标清楚可见。
- `pnpm --filter @personasim/web typecheck` 通过；修改 TSX 的定向 ESLint 与 Prettier 通过。
- `MessageBubble` / `messageDelivery` 13 个测试通过。
- `correspondenceMutations` / `CorrespondencePrimitives` 20 个测试通过。
- IME、历史滚动、启封中切 motion 的独立 E2E 结果由本目录整体实施验收记录汇总。

## 实际限制

390×500 是浏览器短画幅检查，不能替代物理手机软键盘。未在这份记录中宣称真机性能、全设备帧率或现场 Core Web Vitals 已通过。信件未启封隐私检查由独立回归覆盖，截图只使用允许阅读的用户方向草稿。
