# DeepSeek 42 轮产品生活长测：缓存计量与内容质量复核

- 用户授权：先要求“调用 deepseek 进行测试”，随后明确“批准进行长程测试”。
- 完整运行时间：2026-09-06 15:36:57–15:41:01（Asia/Shanghai）；应用内模拟 2026-09-05 至 2026-10-20，共 45 天。
- 完整运行代码：`84558d7`；前置改造见 [缓存实施记录](2026-09-06-provider-prompt-cache-implementation.md) 和 [5 次真实调用验证](2026-09-06-deepseek-prompt-cache-live-test.md)。
- 结论：**42/42 轮执行完成，缓存计量和完成后恢复核对通过；内容质量复核不通过，完整因果链未覆盖。** 不把执行成功或数据库引用有效等同于语义正确。

## 配置与范围

角色顾澜与模拟用户林舟均使用官方 `https://api.deepseek.com` 的 `deepseek-v4-flash`，凭据取现有 `legacy` 配置；实际响应模型名也均为 `deepseek-v4-flash`。这是同一模型承担两个角色的合成对话，不是两个独立模型的对照，也没有真实用户生活结果。

进程内设置非思考、非流式，角色输出上限 8,192 tokens，模拟用户上限 4,096，单次超时 120 秒，Provider 最多重试 1 次。没有修改 `.env` 或持久化默认模型参数。沿用 `stable-prefix-v1` 消息布局与 `named-public-history-v4` 输入协议，没有填充无用 token。

完整运行调用现有 `runProductLifeLongRun`，通过应用 HTTP 注入接口执行角色创建、编辑、恢复版本、发布、42 轮对话、书信投递和读取、跨日激活、新会话、检查点、自传和产物检查。第 10 天、第 45 天重开同一数据库；另有对照数据库检查。图片使用 fixture，不评估真实图像生成或前端浏览器交互。

为了在有限轮次触发上下文压缩，沿用 runner 的缩短阈值：完整原文保留 24 小时、soft 2,400 / hard 4,800 tokens、最小尾部 1,200 tokens、最近 6 轮对话（`minimumRecentTurns=6`，仍受 hard limit 约束）。因此不能把此结果直接推广到默认保留窗口或真实等待 45 天。

执行前保存 `execution-plan.json`。每次运行最多 200 次外部物理请求，两次长测合计按 2 美元估算上限约束；第二次预算已计入第一次的 0.044760928 美元。HTTP 包装器只允许官方 DeepSeek 主机，在请求前预留成本，记录所有尝试和返回用量。上限是本地按 token 单价估算的预算约束，不是账户扣费硬限额。

## 首次中断、最小修复与重新执行

首次目录：`tmp/deepseek-long-run/deepseek-cache-life-20260906-01/`，代码 `2e4930c`。完成 21 轮后，写信步骤两次返回以独立一行“顾澜：”开头的正常信件，均被 `inspectProductLifeUserText` 的角色标签正则误判为 `simulated_user_writes_character_dialogue`，最终中断。

这是测试输入检查器的误报。两封信的 Provider 调用均成功，其费用仍须计入；原始信件与拒绝记录保留在首次目录的 `run/model-io.jsonl` 和 `run/user-input-checks.jsonl`。

修复以 `84558d7` 单独提交：

- `product-life-long-run-input.ts` 增加默认 `chat` 的文本种类；只有 `letter` 允许开头独立收件人行。检查时跳过该称呼，不修改生成内容；正文、后续行角色标签与自我称呼检查继续生效。
- `product-life-long-run.ts` 仅在模拟用户写信调用处传入 `letter`，不改变提示词、Schema 或普通聊天检查。
- 17 项新增用例覆盖中英文冒号、空白、CRLF、内嵌和后续角色台词、自我称呼等；完整流程 fixture 改为使用该称呼，验证调用处确实选择信件模式。

定向验证共 **2 个文件、35 项测试通过**，包括完整 42 轮、完成后恢复和第 45 天中断恢复；Server 类型检查、相关 ESLint、Prettier 和 `git diff --check` 通过。

第二次使用全新目录 `tmp/deepseek-long-run/deepseek-cache-life-20260906-02/`，固定修复后的提交重新执行，没有将不同代码版本混入一个长测。前后两次均保留，不删除失败样本。

## 完整运行计量

99 次物理尝试对应 96 次 Provider 逻辑调用，全部 HTTP 200、`finish_reason: stop`。其中 95 次通过 Provider 结构校验，4 次结构校验失败，失败产生的用量全部保留：角色聊天 3 次失败后在原 `logicalCallId` 内重试；模拟用户第 32 轮第一次失败后由应用发起新逻辑调用。另有 3 次 `repair_chat_turn`，它们是应用修复调用，单列用途。

| 用途                         | 物理尝试 | 逻辑调用 | Provider 失败 | Provider 重试 |  输入 token |  读取 token | 输出 token |        估算 USD |
| ---------------------------- | -------: | -------: | ------------: | ------------: | ----------: | ----------: | ---------: | --------------: |
| compile_character            |        1 |        1 |             0 |             0 |       6,345 |       6,272 |      3,894 |     0.002630004 |
| chat_turn                    |       45 |       42 |             3 |             3 |     395,368 |     185,984 |     13,038 |     0.055971448 |
| checkpoint_autobiography     |        5 |        5 |             0 |             0 |      19,102 |       4,480 |        559 |     0.003617140 |
| letter_reply                 |        1 |        1 |             0 |             0 |      31,913 |           0 |        301 |     0.007219520 |
| repair_chat_turn             |        3 |        3 |             0 |             0 |       7,778 |       2,304 |        496 |     0.001547768 |
| simulate_product_life_user   |       43 |       43 |             1 |             0 |     418,693 |     352,640 |      3,755 |     0.019478440 |
| simulate_product_life_letter |        1 |        1 |             0 |             0 |       9,812 |         256 |        162 |     0.002211032 |
| **合计**                     |   **99** |   **96** |         **4** |         **3** | **889,011** | **551,936** | **22,205** | **0.092675352** |

角色侧 55 次物理尝试，缓存读取率 **43.22%**，估算 0.070985880 美元；模拟用户侧 44 次，读取率 **82.36%**，估算 0.021689472 美元。合计读取率 **551,936 / 889,011 = 62.08%**。这是 token 加权读取率，不能当作“命中请求数占比”。

原始 HTTP usage 与两侧 Provider 指标按输入、输出、读取 token 的多重集完整对应，零缺失、零重复计入。99 个响应均满足原生读取字段与兼容别名相等、读取加未命中等于输入、输入加输出等于总用量。缓存读取字段覆盖 **99/99**，写入字段覆盖 **0/99**；写入保持未知，不把未命中量当作写入量。

### 前缀证据与例外

角色聊天的 JSON system 指令、角色 system、user Schema 三项在全部 42 轮和重开之间各只有一个 hash。Provider 诊断在第 1、22、36 轮重新建立相邻请求比较基线，后两次对应数据库重开；历史滚动和当前状态变化仍发生在动态消息中。

模拟用户第 33、36 轮刻意采用枚举式、不给出答案的回忆问题 Schema，因此同用途 Schema 有 3 个 hash。这两个探针分别只读取 256 / 14,882、256 / 15,992 tokens。第 34、37 轮恢复普通 Schema，与紧邻请求的公共前缀仍只有 681 字符，但实际分别读取 13,824、14,976 tokens。这说明相邻字符前缀诊断不能预测供应商是否复用了更早请求。

第二次首个角色编译请求已读取 6,272 / 6,345 tokens，可能复用了第一次缓存。因此两次运行不能称为独立冷缓存 A/B。官方也说明缓存为尽力而为、输出仍受随机性影响：[DeepSeek 缓存规则](https://api-docs.deepseek.com/guides/kv_cache/)。此次未做旧布局配对、TTL 过期、默认 thinking 模式或 Qwen/GLM 对照，不能据此宣称改造带来了确定的长期降费或质量变化。

### 两次长测累计费用

| 范围                | 物理尝试 |    输入 token |    缓存读取 |  未命中输入 | 输出 token |        估算 USD |
| ------------------- | -------: | ------------: | ----------: | ----------: | ---------: | --------------: |
| 第一次：21 轮后中断 |       50 |       335,047 |     194,944 |     140,103 |     19,051 |     0.044760928 |
| 第二次：42 轮完成   |       99 |       889,011 |     551,936 |     337,075 |     22,205 |     0.092675352 |
| **本次长测总计**    |  **149** | **1,224,058** | **746,880** | **477,178** | **41,256** | **0.137436280** |

依据 [2026-09-06 查阅的官方价格](https://api-docs.deepseek.com/quick_start/pricing/)，周日为 off-peak，每百万 token 的美元单价为读取 0.007、未命中输入 0.22、输出 0.66。逐次使用 `(读取×0.007 + 未命中×0.22 + 输出×0.66) / 1,000,000` 复算。两次运行 5 个 Provider 失败尝试共 0.008801644 美元，首次被上层误拒的两封信共 0.002461444 美元，均已包含。

这是返回用量乘官方价的估算，**未与账户实际扣款对账**。此前单独 5 次 smoke 的 0.002670108 美元不属于上述长测；若合并此前 smoke，则本会话这两批 DeepSeek 实验共 154 次、估算 0.140106388 美元。

## 恢复、结构检查与未覆盖项

完整运行的 `acceptance-status.json` 为执行完成，30 项产品功能检查通过，无失败、无功能检查未覆盖。该文件的语义状态仍是 `pending_review`，不是内容质量通过证明；本报告补充独立语义复核结论，不回写运行器原始状态。

主库有 84 条聊天消息及对应归档、3 个会话、5 个已提交检查点、5 个自传快照、3 条 active memory。数据库结构审计中已经触达的引用检查均通过。

书信在 D3 寄出，D8 抵达的历史快照在 D10 处理，回信 D13 到达、D15 启封。D10 普通接口不返回正文、提前启封返回 409；D15 才向模拟用户公开正文。D45 重新打开后，公开历史仍只有一条回信，首次可见时间保留为 D15。回信没有使用 D10 才首次出现的“岗亭那盏灯”情节。另有一件模板纪念物通过来源和资产检查，主图/缩略图返回非空 WebP，hash 与持久元数据一致；分享预览正文关闭。它不代表真实多模态生成通过。

但 `decision_records`、`action_records`、`outcome_records`、`reflection_records`、`relationship_milestones` 均为 0。决策、行动、结果、反思、关系里程碑引用，以及 user / character / shared 三类完整因果链，共 **8 项结构检查为 `not_reached`**。派生的 28 条 `life_outcomes` 不能替代发生证据支持的 `outcome_records`，不能据此把完整因果链验收标为成功。

在固定 `84558d7` 的新进程中执行完成后 `resume`，完成前 guard 明确阻止任何新付费请求。恢复仍返回 completed / 42，物理请求 **99 → 99**，费用不变。恢复前后比较：

- 18 项账本/产物摘要完全相同，包括模型 IO、两侧指标、HTTP 用量、对话、journal、汇总和最终审计；缺失文件状态也相同。
- 主库与对照库各 67 张表的排序行内容 hash、行数，以及各 307 个 schema entry 的 hash 完全相同。
- 快照工具用数据库字节的只读内存副本，要求 WAL 已清空；只输出计数和摘要，不记录数据行或密钥。

这验证了本次完成后恢复未重复调用或改变数据库内容；不替代任意进程崩溃位置、网络故障或并发恢复测试。

## 语义复核：不通过

### 更正事实没有进入后续回答上下文

第 8 轮用户将画画时间从周四更正为**周二**，角色明确回复“我记下了，是周二”。第 33 轮采用不给答案的问题探针，角色却回复“每周三傍晚”；第 34 轮用户再次纠正。

证据链来自 `turn-08.json`、`turn-33.json`、`turn-34.json`、第 33 轮实际 `model-io.jsonl` 和 `checkpoint-phase-4.sqlite`：

- 周二确认仍保存在自传 entry / event card，来源消息为 `message_g4-0Gpzth_S_Gxmn_pq7q`；并非数据库所有证据都已丢失。
- 持久 memory 中没有画画星期事实。第 33 轮记忆检索 abstain，分数约 0.346086，selected IDs 为空。
- 第 33 轮实际 system + prompt 不包含周二、周三或周四；最近原文只有第 23–32 轮，注入的自传 revision 4 主要覆盖第 11–13 轮。
- 对真实用户文本离线重放 `extractExplicitWeeklyPlanFacts`，第 3、8、34、39、42 轮均返回空数组，确认当前有限正则未识别这些表达。

对应代码与后续修改方向：`packages/features/src/memory-claim.ts` 的 `extractExplicitWeeklyPlanFacts`（约 194–226 行）及 `apps/server/src/services/turn-decision-service.ts` 的候选提取路径；`memory-recall-hierarchy.ts` 的最终 abstention；`conversation-service.ts` 在 abstain 时不提供 memory evidence；`conversation-context-service.ts` 和 `continuity-repository.ts` 只取 latest autobiography snapshot，`prompt-assembler.ts` 的 `compactAutobiography` 对该快照压缩。

下一批应先用上述真实表达建立离线失败用例，改进明确计划与否定更正的提取及旧值替代，再回放检索和快照投影，确认正确事实实际进入后续 prompt。同时扩展 `explicit-fact-reply-guard.ts` 的单事实星期回忆契约，约束缺乏证据时承认不确定。此次证据不足以断言只改正则就能修复检索排序、阈值或快照覆盖；不应仅降低召回阈值或全部塞回历史，也不能将无依据回答归因于供应商缓存。

### 模拟用户事实污染与角色顺从

- 第 24 轮用户声称“早上看到你上周那封信”，角色接着确认。在该时刻回信尚未可读：用户模型的公开历史只有林舟自己寄出的信，`phase-3-letters` 的公开消息为空，回信应在 2026-09-18 到达，第 24 轮时刻为 2026-09-15。接口未泄漏回信正文；模型虚构了已读经历，角色没有纠正。
- 第 26 轮用户把顾澜的影片“灯”的内容说成“我这边灯的事”；第 37 轮进一步声称自己在处理《夜航》粗剪，并询问角色原本属于林舟的外包决定。角色接受归属反转，还回答“那个外包最后没接”；第 38 轮继续沿受污染前提叙述粗剪进展。
- 第 3 轮用户称“今天周日”，但模拟日期 2026-09-05 是周六；第 27 轮把同一天第 26 轮的内容说成“昨天”。这些是输入质量缺陷，不能混同 API 时钟错误。

现有 `inspectProductLifeUserText` 只识别明确自我称呼、行首角色标签等形式；本次信件修复仅纠正一个误报，并不提供语义事实校验。后续应基于公开历史的角色、source ID 和首次可见时间，对归属及已发生断言建立校验与修复，并在角色侧避免把用户无证据断言直接当作已有事实。

### 连续性与因果链边界

第 36 轮新会话能提到角色的影片和用户未决外包，属于部分连续性证据；实际 recentVerbatim 为空，memory recall 仍 abstain（约 0.284922），依靠注入自传回顾。影片“岗亭灯”更接近较早第 22 轮，未跟进第 30、34 轮后续表述，因此不能称为完整准确的长期记忆召回。

第二次运行中用户多次表示外包消息尚未发送、散步尚未完成，零行动/结果记录在这些位置属于未覆盖，不能套用第一次运行的完成事实。第 14 轮则明确把“今晚散步还是整理照片”的小决定委托给角色，角色选择散步，而 phase 1 检查点没有 decision record；这是需后续核对的委托决策落库缺口。用户外包 dilemma 的摘要还混入第 13 轮询问角色自身取舍的内容，需要检查主题和主体关联。

首次运行另有《夜航》归属反转，且用户明确自报完成散步后仍无行动/结果记录的证据；与第二次分别保留，不跨运行拼接为一条因果链。

最终三条持久 memories 中另有两条误提取：第 19 轮“脑子里还在转画面”和第 29 轮“你那边夜班司机的素材还在理着”等普通关心，被写成“用户的……现在存放在……”的物品存放事实。对应 `memory-claim.ts` 约 436 行的提取与 `turn-decision-service.ts` 约 1770 行的 candidate 生成；下一批需覆盖“还在”表示持续动作、心理状态及不同主体的负例，不能把引用结构有效视为事实内容正确。

## 产物与复核入口

完整运行目录 `tmp/deepseek-long-run/deepseek-cache-life-20260906-02/`：

- `execution-plan.json`、`driver-result-run.json`、`driver-result-resume.json`：配置、代码版本、执行结果。
- `http-attempts.jsonl`、`http-usage.jsonl`：真实物理请求及用量；不包含 Authorization 或完整请求正文。
- `run/conversation.md`、`run/turn-*.json`、`run/model-io.jsonl`：可读对话、逐轮证据及合成模型输入输出。
- `run/provider-metrics.json` / `.md`、`analysis.json`：用途分组计量、字段覆盖和审计汇总。
- `run/acceptance-status.json`、`run/final-audit.json`、`run/phase-*-*.json`、`run/checkpoint-phase-*.sqlite`：功能检查与数据库证据。
- `resume-snapshot-before.json`、`resume-snapshot-after.json`、`resume-verification.json`：恢复前后无新调用、产物与表内容不变的核对。

临时驱动、汇总和恢复快照脚本分别为 `tmp/deepseek-product-life-driver.ts`、`tmp/deepseek-product-life-summary.ts`、`tmp/deepseek-product-life-resume-snapshot.ts`。原始产物与脚本位于 Git 忽略目录，报告纳入版本控制；再次执行 run 会创建新目录并产生新费用，resume 要求代码提交与执行计划一致。不要将含 `.instance-secret` 的完整运行目录直接对外发布。
